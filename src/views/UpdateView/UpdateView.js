import React from "react";
import axios from "axios";
import { Box } from "@material-ui/core";

const UpdateView = () => {

    // 신규 화장실 등록, 삭제, 수정..?
    const axiosUpdate = () => {

    }

    // 버튼 클릭 이벤트 (update)
    const onClickBtn = () => {

    };

    return <Box className="view-frame">
        update view
        기존에 존재하는 화장실 위치 수정, 삭제, 새로 추가.. 등의 기능이 필요할 듯?
    </Box>
};

export default UpdateView;